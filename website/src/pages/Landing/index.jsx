import React, { Component } from "react";
import { Typography } from "@material-ui/core";

class Landing extends Component {
  state = {};
  render() {
    return (
      <section>
        <Typography variant="h3">Welcome to Quantify</Typography>
      </section>
    );
  }
}

export default Landing;
